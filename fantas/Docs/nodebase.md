# fantas.nodebase

> fantas 树形节点模块

在 fantas 中，UI 元素是以树形结构组织的，此模块提供一个基本的树形节点类，实现了指针域的一些基本方法，如果想要得到一个可用的树形节点，需要通过子类继承，然后由子类实现数据域的部分。

## fantas.Nodebase

树形节点基类，包含指针域的相关实现。
`Nodebase() -> Nodebase`

属性：

- 
